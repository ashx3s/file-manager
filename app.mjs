"use strict";
import * as fs from "node:fs/promises";
import * as path from "node:path";

// mkdir -- accepts dirname and subpaths

// ls -- lists contents of current directory unless otherwise specified

// prompt for validation to commit all files -- take user input y/n or yes/no

// exit gracefully command -- run exit to shut everything down

// interactive console -- take input from user to perform operations and give helpul details when absolutely necessary
